{
  "name": "eternium-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "node tests/api-tests.js",
    "deploy": "node scripts/deploy.js",
    "deploy:raw": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
